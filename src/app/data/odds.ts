export enum odds {
    likely = 'Likely',
    average = 'Average',
    unlikely = 'Unlikely',
}

export enum amounts {
    more = 'Likely Higher',
    average = 'Average',
    fewer = 'Likely Lower'
}